name: Deploy Frontend to GitHub Pages

on:
  push:
    branches: [ main ]
    paths:
      - 'apps/frontend/**'
      - '.github/workflows/deploy-frontend.yml'

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: apps/frontend/package-lock.json

      - name: Install Dependencies 🔧
        run: |
          cd apps/frontend
          npm install

      - name: Build 🏗️
        run: |
          cd apps/frontend
          npm run build
        env:
          VITE_API_URL: https://geology-rain-studying-internationally.trycloudflare.com
          VITE_SUPABASE_URL: https://rwcozsavcafomcqmkzqo.supabase.co
          VITE_SUPABASE_ANON_KEY: sb_publishable_I83gwH3ihoc2dCQFNgdb7A_YhTszilm

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: apps/frontend/dist
          branch: gh-pages
